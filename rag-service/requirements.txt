# RAG Service dependencies

fastapi==0.115.5
uvicorn==0.32.1
python-dotenv==1.0.1
python-multipart==0.0.19

# pdfplumber — extracts text from the uploaded application PDF
pdfplumber==0.11.4

# openai — the Python SDK for OpenAI-compatible APIs (Requesty uses this format)
# Even though we're using Requesty, the openai library works because
# Requesty is OpenAI-compatible (same API format, different URL)
openai==1.58.1

# chromadb — vector database for storing and searching law paragraph embeddings
# This runs entirely in-memory (no separate server needed for the demo)
# It's the "search engine" that finds relevant BayBO paragraphs
chromadb==0.5.20

# tiktoken — counts tokens in text, used to stay within LLM context limits
tiktoken==0.8.0
